import Head from "next/head";

const Header = () => {
  return (
    <Head>
      <title>
        Luminous Algo - Visualize algorithms with a simple drawing interface
      </title>
      <meta name="description" content="App" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Header;
